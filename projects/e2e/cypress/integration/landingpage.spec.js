context('LandingPage', () => {
  it('[CTA] User can navigate to search-page', () => {})
  it('[CTA] User can navigate to create-project-page', () => {})
  it('[CTA] User can subscribe to newsletter', () => {})
})
